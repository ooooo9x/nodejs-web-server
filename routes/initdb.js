var express = require('express');
var router = express.Router();
//引入ORM框架
var dbUtils = require('../src/utils/dbUtil');

/* GET users listing. */
// router.get('/', function(req, res, next) {
//   dbUtils.initdb();
// });

module.exports = router;
